---
"@revealui/knowledge-graph": patch
"@revealui/mcp": patch
---

Extract `assembleContext` into `@revealui/knowledge-graph` so product `kg_context` and the RevDev bridge share the MIT helper. `/memory` remains part of this package for the next publish.
