---
'@revealui/db': minor
'@revealui/contracts': minor
'@revealui/mcp': minor
---

Record the account on governed MCP tool receipts so new rows anchor per account. Add the MCP approval tables and JCS helpers that bind a call to its tool, argument hash, schema hash, account, and requester. The approval gate is not wired in this slice.
