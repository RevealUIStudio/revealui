---
"@revealui/mcp": patch
---

Add a knowledge-graph MCP toolset with an opt-in product mode: CallTool threads request extras, writes stamp actor and scope from the session principal, and results use a status envelope. Compat mode stays unwrapped for existing callers.
