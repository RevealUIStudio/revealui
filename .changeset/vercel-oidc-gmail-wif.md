---
'@revealui/services': patch
---

Read the Vercel Function OIDC token with getVercelOidcToken so Gmail workload identity federation can send cron alerts. The token arrives on the request, not as a stored VERCEL_OIDC_TOKEN.
