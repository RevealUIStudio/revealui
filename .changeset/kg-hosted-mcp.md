---
"@revealui/mcp": patch
---

Composite knowledge-graph tools onto the governed `/api/mcp` server: kg_* skip REST credentials, write receipts fail-closed before `kg_add_episode`, and hosted principals stamp account-scoped identity.
