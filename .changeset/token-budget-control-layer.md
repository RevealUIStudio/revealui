---
"@revealui/harnesses": patch
---

Own the session compact budget in the control layer and emit the Grok output-cap hook from it. Sessions load that layer first, then the vendor adapter, and create a thin adapter when the vendor has none.
