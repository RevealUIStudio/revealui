---
"@revealui/presentation": minor
---

Add the product-native quartet: `StatusDot`, `VerdictChip`, `AuditLine`, and `ReceiptCard`. These are greenfield RevealUI-native components for the receipt motif — a status dot with an optional reduced-motion-aware pulse, a guardrail verdict chip, a monospace audit ledger line with a copy-on-click reference, and a receipt card that composes audit lines with an integrity footer. All are styled from `--rvui-*` semantic tokens with APG-conformant labeling and zero external UI dependencies. `StatusDot` supersedes the Studio-local version.
