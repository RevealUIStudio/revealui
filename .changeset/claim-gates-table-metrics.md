---
"@revealui/claim-gates": patch
---

Match markdown table `Label | N` inventory claims (and heading `Apps (N)` forms) against live monorepo metrics so the honesty ledger cannot drift from `site.ts` METRICS.
