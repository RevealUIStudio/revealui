---
"@revealui/presentation": patch
---

Public chrome (marketing nav, docs headers) renders the Circuit-R master at a locked 48×48 box — the brand size-floor minimum — instead of the leftover 96px header lock. Overflow clip stays so scale(1.06) cannot paint a scrollbar. Admin auth remains a 112/160 brand panel.
