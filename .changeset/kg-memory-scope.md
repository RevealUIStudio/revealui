---
"@revealui/knowledge-graph": patch
"@revealui/mcp": patch
---

Scope product memory reads in SQL before LIMIT. Hosted non-operators see the memory subgraph only; deny is distinct from empty; graph walks do not hop invisible edges.
