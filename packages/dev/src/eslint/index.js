import config, {
  baseConfig,
  createTypeCheckedConfig,
  ignorePatterns,
  tsFiles,
} from './eslint.config.js'

export { baseConfig, createTypeCheckedConfig, ignorePatterns, tsFiles }
export default config
