{
  "name": "@revealui/mcp",
  "private": true,
  "type": "module",
  "dependencies": {
    "@revealui/config": "workspace:*"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.3.15",
    "pg": "^8.15.6",
    "vitest": "^4.0.18"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  }
}
