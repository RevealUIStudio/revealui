{
  "name": "@revealui/mcp",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@revealui/config": "workspace:*"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.3.15",
    "pg": "^8.18.0",
    "vitest": "^4.0.18"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "dev": "tsc --watch",
    "lint": "biome check .",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "type": "module"
}
