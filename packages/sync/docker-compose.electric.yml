version: '3.8'

services:
  electric:
    image: electricsql/electric:1.0.17
    environment:
      - DATABASE_URL=${ELECTRIC_DATABASE_URL}
      - ELECTRIC_INSECURE=true
    ports:
      - "3001:3000"
    restart: always