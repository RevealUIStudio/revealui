{
  "name": "@revealui/scripts",
  "version": "0.1.2",
  "private": true,
  "description": "Shared utilities for monorepo scripts — file glob, JSON parsing, conventional logging. Internal to RevealUI.",
  "dependencies": {
    "@revealui/contracts": "workspace:*",
    "@revealui/core": "workspace:*",
    "@revealui/db": "workspace:*",
    "ret": "^0.5.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "catalog:",
    "@revealui/dev": "workspace:*"
  },
  "exports": {
    ".": "./index.ts",
    "./errors": "./errors.ts",
    "./logger": "./logger.ts",
    "./paths": "./paths.ts",
    "./exec": "./exec.ts",
    "./state": "./state/index.ts",
    "./state/workflow": "./state/workflow-state.ts",
    "./state/adapters/pglite": "./state/adapters/pglite.ts",
    "./state/adapters/memory": "./state/adapters/memory.ts",
    "./validation": "./validation/index.ts",
    "./validation/env": "./validation/env.ts",
    "./validation/database": "./validation/database.ts"
  },
  "peerDependencies": {
    "typescript": "^6.0.3"
  },
  "scripts": {
    "build": "echo 'Source-only package — consumed via tsconfig paths, no build needed'",
    "dev": "echo 'Source-only package — no watch mode needed'",
    "lint": "biome check .",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "type": "module"
}
