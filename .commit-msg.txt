chore(lint): apply biome auto-fixes to unblock CI Quality phase

Joshua approved bundling the pre-existing biome lint cleanup into
this PR to unblock CI's full `biome check .` (pre-push only ran
`--phase=1 --changed` so these were never visible locally on my
branch).

Auto-fix categories (all safe transforms biome ships):
  * lint/correctness/noUnusedImports — remove unused vitest /
    drizzle-orm / schema imports across 6 test files
  * lint/complexity/useSimplifiedLogicExpression — collapse
    `!a || !b` to `!(a?.b)` / `!(a && b)` patterns
  * lint/style/* — minor reformatting (single-quote / double-quote
    consistency, paren removal, etc.)

Adjacent fix: add `// console-allowed` directive to
scripts/validate/raw-sql.ts and scripts/electric-latency-probe/
probe.ts. Sibling validators (claim-drift.ts, empty-catch.ts,
ci-gate.ts) already have it; these two were missing it. The
biome auto-fix shifted line numbers in those files, which made
the pre-commit no-console-log validator re-flag pre-existing
console.log statements. Adding the directive is the documented
fix per scripts/analyze/audit-console.ts.

19 + 2 files touched, all pre-existing issues in code this PR
does not otherwise modify.

After this commit `pnpm lint` reports:
  Found 0 errors, 28 warnings, 4 infos.

Warnings + infos don't block the gate. Remaining warnings are
mostly `lint/suspicious/noArrayIndexKey` and
`lint/complexity/noBannedTypes` (Function-as-type) — they need
manual judgment per call-site and are out of scope for this PR.
