#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Regenerate Supabase types before commit to ensure they're up to date
echo "🔄 Regenerating Supabase types..."
pnpm generate:supabase-types

# Add any updated type files to the commit
git add packages/services/src/supabase/types.ts packages/core/src/generated/types/supabase.ts

echo "✅ Supabase types updated and staged"