name: Release Pro Packages

# Pro package source (packages/ai, mcp, editors, services, harnesses) was moved to
# the private RevealUIStudio/revealui-jv repo as part of the OSS/Pro separation.
#
# To publish Pro packages, trigger the release-pro.yml workflow from revealui-jv.
# This file is intentionally a no-op stub kept for reference.

on:
  workflow_dispatch:

jobs:
  noop:
    name: Pro packages are published from revealui-jv
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: |
          echo "Pro package source lives in RevealUIStudio/revealui-jv (private)."
          echo "Trigger release-pro.yml from that repo to publish @revealui/ai, mcp, editors, services, harnesses."
          exit 1
