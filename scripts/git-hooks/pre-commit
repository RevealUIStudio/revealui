#!/usr/bin/env bash
# Leftover. Live git hooks are husky (package.json "prepare").
# This path is not installed and is not the SSOT.
echo "scripts/git-hooks/pre-commit is leftover; husky is the SSOT." >&2
exit 1
