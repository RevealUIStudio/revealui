{
  "buildCommand": "cd ../.. && pnpm turbo build --filter=marketing",
  "installCommand": "cd ../.. && pnpm install --no-frozen-lockfile",
  "ignoreCommand": "[ -z \"$VERCEL_GIT_PREVIOUS_SHA\" ] && exit 1; cd ../.. && git diff --quiet $VERCEL_GIT_PREVIOUS_SHA $VERCEL_GIT_COMMIT_SHA -- apps/marketing packages pnpm-lock.yaml package.json turbo.json",
  "framework": "nextjs"
}
