/**
 * Home page placeholder.
 *
 * Real Hero, Problem, Persona, Primitives, Proof, PricingTeaser, Demo, Faq,
 * GetStarted, and Footer components arrive in chunk 2 of the marketing
 * migration. This stub exists only so the scaffold builds cleanly.
 */
export function HomePage() {
  return (
    <main className="min-h-screen flex items-center justify-center p-8 text-center">
      <div>
        <h1 className="text-4xl font-bold tracking-tight">RevealUI</h1>
        <p className="mt-4 text-lg text-gray-600">Marketing site migration in progress.</p>
        <p className="mt-2 text-sm text-gray-400">
          Vite + @revealui/router scaffold complete; full landing page port lands in chunk 2.
        </p>
      </div>
    </main>
  );
}
