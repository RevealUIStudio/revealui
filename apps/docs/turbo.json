{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", "scripts/**", "public/**", "../../docs/**"],
      "outputs": ["dist/**", "public/docs/**"]
    }
  }
}
