/**
 * Stub module for @revealui/ai subpaths.
 *
 * Used by Turbopack resolve aliases in CI/OSS builds where the Pro package
 * is absent. Every route handler that imports @revealui/ai uses dynamic
 * import().catch(() => null), so this stub is never actually called at
 * runtime  -  it just satisfies the bundler's module resolution.
 */
export {};
