{
  "buildCommand": "cd ../.. && pnpm turbo build --filter=cms",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "ignoreCommand": "[ -z \"$VERCEL_GIT_PREVIOUS_SHA\" ] && exit 1; cd ../.. && git diff --quiet $VERCEL_GIT_PREVIOUS_SHA $VERCEL_GIT_COMMIT_SHA -- apps/cms packages pnpm-lock.yaml package.json turbo.json",
  "framework": "nextjs",
  "github": { "silent": true }
}
