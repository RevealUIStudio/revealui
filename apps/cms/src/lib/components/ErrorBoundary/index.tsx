/**
 * Re-export from the canonical ErrorBoundary.
 *
 * Both Sentry reporting and structured logging are included
 * in the consolidated implementation.
 */
export { ErrorBoundary, withErrorBoundary } from '@/components/ErrorBoundary';
