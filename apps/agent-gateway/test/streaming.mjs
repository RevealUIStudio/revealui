#!/usr/bin/env node
// Legacy JS streaming test shim — tests were converted to TypeScript.
// Run the TypeScript streaming test instead:
//   node --loader ts-node/esm apps/agent-gateway/test/streaming.ts

console.log('This legacy .mjs streaming test was replaced. Run the TypeScript version: apps/agent-gateway/test/streaming.ts')
process.exit(0)

