#!/usr/bin/env node
// Legacy JS test shim — tests were converted to TypeScript.
// Run the TypeScript test instead:
//   node --loader ts-node/esm apps/agent-gateway/test/providers.ts

console.log('This legacy .mjs test was replaced. Run the TypeScript version: apps/agent-gateway/test/providers.ts')
process.exit(0)
