{
  "buildCommand": "cd ../.. && pnpm turbo build --filter=mainframe",
  "installCommand": "cd ../.. && pnpm install --no-frozen-lockfile",
  "ignoreCommand": "[ -z \"$VERCEL_GIT_PREVIOUS_SHA\" ] && exit 1; git diff --quiet $VERCEL_GIT_PREVIOUS_SHA $VERCEL_GIT_COMMIT_SHA -- apps/mainframe packages pnpm-lock.yaml package.json turbo.json",
  "framework": "tanstack-start",
  "outputDirectory": ".output"
}
